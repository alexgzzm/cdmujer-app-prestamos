import 'package:flutter/material.dart';

class NewCreditPage extends StatelessWidget {
  const NewCreditPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Form(child: SizedBox.shrink());
  }
}
